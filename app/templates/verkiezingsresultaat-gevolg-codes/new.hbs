{{!-- Page to create a new verkiezingsresultaat-gevolg-code --}}
<div class="new">
  <h2 class="title">{{t (humanize 'New verkiezingsresultaat-gevolg-code')}}</h2>

  {{!--
    Actions to save new verkiezingsresultaat-gevolg-code
    or delete new verkiezingsresultaat-gevolg-code.
  --}}
  <div class="actions">
    <button type="button" class="btn save" title="Save new verkiezingsresultaat-gevolg-code" {{action 'save' model}}>Save</button>
    <button type="button" class="btn cancel" title="Cancel" {{action 'cancel' model}}>Cancel</button>
  </div>

  {{!-- Table to show details of verkiezingsresultaat-gevolg-code. --}}
  <table>
    <thead>
      <tr>
        <th>Attribute</th>
        <th>Data</th>
      </tr>
    </thead>
    <tbody>
      {{!-- Creating rows for attributes depending on the type of property --}}
            <tr>
        <td>label</td>
<td align='middle'>{{handle-attribute model=model attribute='label' editable=true}}</td>
      </tr>
      
      
    </tbody>
  </table>
</div>
