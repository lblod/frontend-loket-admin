{{!-- Page to create a new toezicht-account-acceptance-status --}}
<div class="new">
  <h2 class="title">{{t (humanize 'New toezicht-account-acceptance-status')}}</h2>

  {{!--
    Actions to save new toezicht-account-acceptance-status
    or delete new toezicht-account-acceptance-status.
  --}}
  <div class="actions">
    <button type="button" class="btn save" title="Save new toezicht-account-acceptance-status" {{action 'save' model}}>Save</button>
    <button type="button" class="btn cancel" title="Cancel" {{action 'cancel' model}}>Cancel</button>
  </div>

  {{!-- Table to show details of toezicht-account-acceptance-status. --}}
  <table>
    <thead>
      <tr>
        <th>Attribute</th>
        <th>Data</th>
      </tr>
    </thead>
    <tbody>
      {{!-- Creating rows for attributes depending on the type of property --}}
            <tr>
        <td>label</td>
<td align='middle'>{{handle-attribute model=model attribute='label' editable=true}}</td>
      </tr>
      
      
    </tbody>
  </table>
</div>
