{{!-- Page to create a new toezicht-document-authenticity-type --}}
<div class="new">
  <h2 class="title">{{t (humanize 'New toezicht-document-authenticity-type')}}</h2>

  {{!--
    Actions to save new toezicht-document-authenticity-type
    or delete new toezicht-document-authenticity-type.
  --}}
  <div class="actions">
    <button type="button" class="btn save" title="Save new toezicht-document-authenticity-type" {{action 'save' model}}>Save</button>
    <button type="button" class="btn cancel" title="Cancel" {{action 'cancel' model}}>Cancel</button>
  </div>

  {{!-- Table to show details of toezicht-document-authenticity-type. --}}
  <table>
    <thead>
      <tr>
        <th>Attribute</th>
        <th>Data</th>
      </tr>
    </thead>
    <tbody>
      {{!-- Creating rows for attributes depending on the type of property --}}
            <tr>
        <td>label</td>
<td align='middle'>{{handle-attribute model=model attribute='label' editable=true}}</td>
      </tr>
      
      
    </tbody>
  </table>
</div>
