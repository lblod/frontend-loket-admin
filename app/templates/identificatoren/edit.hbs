{{!-- Page to edit identificator --}}
{{#with (changeset model skipValidate=true) as |changeset|}}
<div class="edit">
  <h2 class="title">{{t (humanize 'Edit identificator')}}</h2>

  {{!--
    Actions to save or cancel changes;
    or delete identificator.
  --}}
  <div class="actions">
    <button type="button" class="btn save" title="Save new identificator" {{action 'save' changeset model}}>Save</button>
    <button type="button" class="btn cancel" title="Cancel" {{action 'cancel' changeset model}}>Cancel</button>
    <button type="button" class="btn delete" title="Delete this item" {{action 'delete' changeset model}}>Delete</button>
  </div>

  {{!-- Table to show details of identificator. --}}
  <table>
    <thead>
      <tr>
        <th>Property</th>
        <th>Data</th>
      </tr>
    </thead>
    <tbody>
      {{!-- Creating rows for attributes depending on the type of property --}}
            <tr>
      <td>identificator</td>
      <td align='middle'>{{handle-attribute model=changeset attribute='identificator' editable=true}}</td>
</tr>

        
        

    </tbody>
  </table>
</div>
{{/with}}
