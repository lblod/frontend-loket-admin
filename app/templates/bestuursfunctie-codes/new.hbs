{{!-- Page to create a new bestuursfunctie-code --}}
<div class="new">
  <h2 class="title">{{t (humanize 'New bestuursfunctie-code')}}</h2>

  {{!--
    Actions to save new bestuursfunctie-code
    or delete new bestuursfunctie-code.
  --}}
  <div class="actions">
    <button type="button" class="btn save" title="Save new bestuursfunctie-code" {{action 'save' model}}>Save</button>
    <button type="button" class="btn cancel" title="Cancel" {{action 'cancel' model}}>Cancel</button>
  </div>

  {{!-- Table to show details of bestuursfunctie-code. --}}
  <table>
    <thead>
      <tr>
        <th>Attribute</th>
        <th>Data</th>
      </tr>
    </thead>
    <tbody>
      {{!-- Creating rows for attributes depending on the type of property --}}
            <tr>
        <td>label</td>
<td align='middle'>{{handle-attribute model=model attribute='label' editable=true}}</td>
      </tr>
      
      
    </tbody>
  </table>
</div>
