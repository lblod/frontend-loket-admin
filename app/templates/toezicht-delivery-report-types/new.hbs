{{!-- Page to create a new toezicht-delivery-report-type --}}
<div class="new">
  <h2 class="title">{{t (humanize 'New toezicht-delivery-report-type')}}</h2>

  {{!--
    Actions to save new toezicht-delivery-report-type
    or delete new toezicht-delivery-report-type.
  --}}
  <div class="actions">
    <button type="button" class="btn save" title="Save new toezicht-delivery-report-type" {{action 'save' model}}>Save</button>
    <button type="button" class="btn cancel" title="Cancel" {{action 'cancel' model}}>Cancel</button>
  </div>

  {{!-- Table to show details of toezicht-delivery-report-type. --}}
  <table>
    <thead>
      <tr>
        <th>Attribute</th>
        <th>Data</th>
      </tr>
    </thead>
    <tbody>
      {{!-- Creating rows for attributes depending on the type of property --}}
            <tr>
        <td>label</td>
<td align='middle'>{{handle-attribute model=model attribute='label' editable=true}}</td>
      </tr>
      
      
    </tbody>
  </table>
</div>
