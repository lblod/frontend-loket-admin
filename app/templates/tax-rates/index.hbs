{{!-- Page to list tax-rates --}}
<div class="content">
  <h2 class="title">{{t (humanize 'tax-rates')}}</h2>
  <div class="actions">{{#link-to "tax-rates.new" class="btn new" title="Create new item" tagName="button" }}Create{{/link-to}}</div>
  {{#if model}}
    <table>
      <thead>
        <tr>
          <th class="properties__label">id</th>
          {{sortable-table-header attribute="amount" currentSort=sort}}
{{sortable-table-header attribute="unit" currentSort=sort}}
{{sortable-table-header attribute="base" currentSort=sort}}
{{sortable-table-header attribute="remark" currentSort=sort}}

          
          
        </tr>
      </thead>
      <tbody>
        {{#each model as |item|}}
          {{#link-to "tax-rates.show" item tagName='tr' }}
              <td>{{handle-attribute model=item attribute='id' classes='properties__data'}}</td>
            {{handle-attribute model=item attribute='amount' tagName='td' classes='properties__data'}}
            {{handle-attribute model=item attribute='unit' tagName='td' classes='properties__data'}}
            {{handle-attribute model=item attribute='base' tagName='td' classes='properties__data'}}
            {{handle-attribute model=item attribute='remark' tagName='td' classes='properties__data'}}


          {{/link-to}}
        {{/each}}
      </tbody>
    </table>

    {{#if model.meta.pagination}}
      {{pagination-component page=page numberOfItems=model.length total=model.meta.count links=model.meta.pagination}}
    {{/if}}
  {{else}}
    <div class="empty">
      No {{t (humanize 'tax-rates')}} in the database.
      Click on the <i class="fa fa-plus" aria-hidden="true"></i> button to create a new {{t (humanize "tax-rate")}}.
    </div>
  {{/if}}
</div>
