{{!-- Page to create a new toezicht-inzending-type --}}
<div class="new">
  <h2 class="title">{{t (humanize 'New toezicht-inzending-type')}}</h2>

  {{!--
    Actions to save new toezicht-inzending-type
    or delete new toezicht-inzending-type.
  --}}
  <div class="actions">
    <button type="button" class="btn save" title="Save new toezicht-inzending-type" {{action 'save' model}}>Save</button>
    <button type="button" class="btn cancel" title="Cancel" {{action 'cancel' model}}>Cancel</button>
  </div>

  {{!-- Table to show details of toezicht-inzending-type. --}}
  <table>
    <thead>
      <tr>
        <th>Attribute</th>
        <th>Data</th>
      </tr>
    </thead>
    <tbody>
      {{!-- Creating rows for attributes depending on the type of property --}}
            <tr>
        <td>label</td>
<td align='middle'>{{handle-attribute model=model attribute='label' editable=true}}</td>
      </tr>
      
      
    </tbody>
  </table>
</div>
