{{!-- Page to create a new toezicht-tax-type --}}
<div class="new">
  <h2 class="title">{{t (humanize 'New toezicht-tax-type')}}</h2>

  {{!--
    Actions to save new toezicht-tax-type
    or delete new toezicht-tax-type.
  --}}
  <div class="actions">
    <button type="button" class="btn save" title="Save new toezicht-tax-type" {{action 'save' model}}>Save</button>
    <button type="button" class="btn cancel" title="Cancel" {{action 'cancel' model}}>Cancel</button>
  </div>

  {{!-- Table to show details of toezicht-tax-type. --}}
  <table>
    <thead>
      <tr>
        <th>Attribute</th>
        <th>Data</th>
      </tr>
    </thead>
    <tbody>
      {{!-- Creating rows for attributes depending on the type of property --}}
            <tr>
        <td>label</td>
<td align='middle'>{{handle-attribute model=model attribute='label' editable=true}}</td>
      </tr>
      
      
    </tbody>
  </table>
</div>
